<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alex.secondhandcarseller">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />

        <activity android:name=".DealerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AgentActivity" />
        <activity android:name=".EditCarActivity" />
        <activity android:name=".AddCarActivity" />
        <activity
            android:name=".AddPhotoActivity"
            android:parentActivityName=".AddCarActivity" />
        <activity android:name=".AgentDetailActivity" />
        <activity android:name=".AddAgentActivity" />
        <activity android:name=".BookingDetailActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".DealerListActivity" />
        <activity android:name=".AdminDealerDetail" />
        <activity android:name=".DealerRegisterActivity" />
        <activity android:name=".EditDealerProfile" />
        <activity android:name=".PromotionActivity" />
        <activity android:name=".ScanActivity" />
        <activity android:name=".ResetPwActivity"></activity>
    </application>

</manifest>